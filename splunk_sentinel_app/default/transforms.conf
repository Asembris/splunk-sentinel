# Splunk Sentinel - Field Transformations
# Sourcetype definitions for Sentinel data

[sentinel_investigation_fields]
REGEX = investigation_id=(?P<investigation_id>[^\s]+)
FORMAT = investigation_id::$1
